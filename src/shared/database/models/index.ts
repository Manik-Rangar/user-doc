import { Document } from './document.model';
import { OTP } from './otp.model';
import { Role } from './role.model';
import { User } from './user.model';

export const models = [User, Role, OTP, Document];

export { User, Role, OTP, Document };
